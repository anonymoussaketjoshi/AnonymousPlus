<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.saket.attendanceplus">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:name=".myCustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main_login"
            android:label="@string/title_activity_main_login" />
        <activity android:name=".select_course" />
        <activity android:name=".action_menu" />
        <activity
            android:name=".attendance_session"
            android:label="@string/title_activity_attendance_session" />
        <activity android:name=".settings" />
        <activity android:name=".settings_professor" />
        <activity android:name=".settings_course" />
        <activity android:name=".googleapipage" />
        <activity android:name=".tabViewActivity"
            android:theme="@style/NoActionBar">

        </activity>
    </application>

</manifest>